import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'at.wifi.codingacademy.apps.stronks',
  appName: 'stronks',
  webDir: 'www'
};

export default config;
